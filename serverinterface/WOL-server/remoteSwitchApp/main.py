import backend
import frontend


#frontend:
    #open gui for socket switching
    #have buttons to open individual interfaces for each device (octoprint, parsec, VNC)
    #get online time counter from arduino

#backend:
    #ssh to synology or weather station with Paramiko
    #have telnet commands to arduino
    #have WOL to server

